{
  "name": "urdu-tools",
  "private": true,
  "version": "1.3.1",
  "description": "Urdu text processing monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:coverage": "pnpm -r test:coverage",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
